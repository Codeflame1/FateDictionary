<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.type_moon.codeflame.fatedictionary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/sanguoicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/sanguoicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Character.CharacterAdd"/>
        <activity android:name=".Skill.SkillAdd"/>
        <activity android:name=".Skill.SkillEdit"/>
        <activity android:name=".Character.CharacterDetail"/>
        <activity android:name=".Skill.SkillDetail"/>
        <activity android:name=".LittleTest"/>
        <activity android:name=".Character.CharacterEdit">

        </activity>
        <service android:name=".MusicService"/>
    </application>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>

</manifest>